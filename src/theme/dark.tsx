import { createTheme, ThemeOptions } from '@mui/material/styles'

export const light: ThemeOptions = createTheme({
  palette: {
    primary: {
      main: '#FFFFFF'
    },
    success: {
      main: '#FFFFFF'
    }
  }
});